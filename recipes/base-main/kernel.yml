modules:
  - type: containerfile
    snippets:
      - COPY --from=ghcr.io/ublue-os/bazzite-kernel:41 /tmp/rpms /tmp/kernel-rpms
      - RUN rpm-ostree override replace --experimental /tmp/kernel-rpms/kernel-**.rpm
  - type: rpm-ostree
    repos:
      - https://copr.fedorainfracloud.org/coprs/ublue-os/staging/repo/fedora-%OS_VERSION%/ublue-os-staging-fedora-%OS_VERSION%.repo?arch=x86_64
    remove:
      - firefox
      - firefox-langpacks
      - htop
      - ublue-os-update-services
    install:
      # Kernel
      - scx-scheds
      #- ublue-update